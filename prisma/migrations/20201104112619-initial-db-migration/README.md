# Migration `20201104112619-initial-db-migration`

This migration has been generated by Mark Davis at 11/4/2020, 11:26:19 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "User" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "email" TEXT NOT NULL,
    "firstName" TEXT NOT NULL,
    "lastName" TEXT NOT NULL,
    "profileId" INTEGER,

    FOREIGN KEY ("profileId") REFERENCES "Profile"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "Distance" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "metres" INTEGER NOT NULL
)

CREATE TABLE "Profile" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "time" REAL NOT NULL,
    "distanceId" INTEGER NOT NULL,

    FOREIGN KEY ("distanceId") REFERENCES "Distance"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201104112619-initial-db-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,35 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "sqlite"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id        Int      @id @default(autoincrement())
+  email     String   @unique
+  firstName String
+  lastName  String
+  profile   Profile? @relation(fields: [profileId], references: [id])
+  profileId Int?
+}
+
+model Distance {
+  id      Int       @id @default(autoincrement())
+  name    String
+  metres  Int
+  Profile Profile[]
+}
+
+model Profile {
+  id         Int      @id @default(autoincrement())
+  distance   Distance @relation(fields: [distanceId], references: [id])
+  time       Float
+  User       User[]
+  distanceId Int
+}
```


